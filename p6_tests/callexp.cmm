bool fn_b(bool b) {

}

int fn_i() {
    return 2 + 2;
}

short fn_s() {
    ptr short s;
    read @s;
    return @s;
}

void v(int a, int b, string c, short d, bool e, ptr string f, string g){
    return;
}

int main() {
    int a;
    string str;
    v(1, 3S, "abc", 4S, false, &str, str);

    fn_i();

    a = fn_i();

    a = fn_i() + a;

    int b; 
    ptr int p;

    b = fn_s() - 3S * (fn_i() / fn_s()) + @p;

    write a;
}